# Mission: 深入学习 Linux 内核，晋升内核 / BSP 工程师

## Why
从"能照着框架做驱动移植"，升级为"能独立设计内核模块、定位内核级问题、读懂社区子系统讨论"。这样就能拿到内核 / BSP 岗位的入场券，而不是只能做应用层和移植。

## Success looks like
- 能读懂 `dmesg` 里的 oops/panic 调用栈，独立定位一个内核级 bug
- 从零写出一个可加载的内核子系统 / 驱动框架（不是移植现成的）
- 看懂 LWN.net 上的子系统深度文章和 patch 评审讨论
- 说清内核三大核心（内存管理 / 文件系统 / 进程管理）的关键机制，以及它们如何支撑自己熟悉的驱动场景

## Constraints
- 中文教学
- 主教材：《图解Linux内核（基于6.x）》（姜亚华，机械工业出版社，2024，对应 Linux 6.2）
- **【课程结构要求】课程必须与书本目录对齐，且对齐到三级标题**：书目录的每个章节（§X.Y）和**每个三级子节（§X.Y.Z）**都要在课程里有明确归属，目录标题要能被逐级"串起来"。具体落实为三点：
  1. 维护一张对照表 `CURRICULUM_MAP.md`：**逐个三级子节**列出书目录 ↔ 课程编号 ↔ 覆盖状态，发现缺口时即时更新。
  2. 每个课件页眉的"chip"要标对应章节号（如"§2.1"），并列出所覆盖的**三级子节**（如"§2.1.1 一对一 / §2.1.2 一对多 / §2.1.3 多对多"）。
  3. 三级子节是覆盖判定的最小单位：一个子节要么 ✅ 已覆盖、要么 🟡 部分（注明缺哪个点）、要么 ⬜ 未覆盖——不允许"在章节级一笔带过"就标成覆盖。
- 有 Linux / QEMU 环境，可以做真实内核实验（编译模块、insmod、ftrace 等）
- 已有背景：C 和单片机扎实；有蓝牙 / WiFi 驱动移植经验；对内核驱动框架与机制缺乏深入理解
- 每课短小、可快速完成；用"MCU 思维 ↔ 内核思维"对照帮助迁移

## Out of scope
- 向 upstream 提交 patch 的社区流程（未来可加，现阶段先打基础）
- 实时子系统（PREEMPT_RT）—— 待三大核心学完再考虑
- 非 x86/ARM 体系结构的细节
