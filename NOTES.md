# NOTES — 用户偏好与教学笔记

## 用户背景与思维起点（LR-0002 修正）
- **扎实**：C 语言（C99 熟练，for 内声明变量）、单片机（裸机 / 寄存器思维）
- **有经验**：嵌入式 Linux 蓝牙驱动、WiFi 驱动**移植**
- **工程素养很高**：独立搭建 WSL2 内核编译环境并写成 423 行踩坑文档；用 git + GitHub Pages 托管整个学习项目；带 README、提交规范。**不是初学者水平的严谨度。**
- **结果**：之前低估了。后续课程可放心加深、加大实验、减少 hand-holding。

## 动手环境（LR-0002 确认）
- **实际是 WSL2 + Linux 6.6.87.2-microsoft-standard-WSL2**，不是 QEMU。
- 用户自建搭建文档：`doc/WSL2内核编译与驱动环境搭建.md`（项目 doc/ 目录）。
- **WSL2 限制**：无真实硬件透传，USB/PCI/中断 DMA/GPU 类驱动跑不了。
  → 纯软件模块（list/hlist/rbtree/timer/sysfs/proc/字符设备）可跑；硬件相关实验要标注"WSL2 不可跑，理论或留真机"。
- 用户代码风格倾向：`uint16_t`（stdint）而非内核 `u16`——后续可引导统一到内核风格，但不强求。

## 教学偏好
- **语言**：中文。代码注释中英均可，关键术语保留英文原词（如 `task_struct`、`page fault`）并附中文。
- **思维迁移**：每课尽量做"MCU / 裸机 怎么做 ↔ 内核 怎么做"的对照表或图。这是用户最大的迁移杠杆。
- **节奏**：每课短小、可一次完成；先给"最小够用的知识"，立刻进入一个能跑起来的动手实验（tangible win），再 quiz 巩固。
- **难度**：在最近发展区内——既不能是他已经会的，也不能跳过太多前置。**LR-0002 修正：用户严谨度被低估，可适当加深。**
- **动手环境**：WSL2 + Linux 6.6.87.2（见用户自建文档 `doc/WSL2内核编译与驱动环境搭建.md`）。鼓励每课附可编译的 `.ko` 模块或可跑的命令，避免纯理论。硬件相关实验明确标注 WSL2 限制。

## 课程主线（与本书章节对齐）
1. **知识储备篇（§1–4）**：数据结构 / 中断 / 时间 —— 打通 MCU→内核 思维。**← 当前阶段**
2. **内存管理篇（§5–9）**：驱动开发的头号痛点，对用户价值最高。
3. **文件系统篇（§10–12）**：VFS / sysfs / ext4。
4. **进程管理篇（§13–16）**：进程 / 调度 / IPC / 信号。
5. **综合应用篇（§17–23）**：ELF/epoll/Binder、**设备驱动模型 + V4L2**（回到用户熟悉的驱动领域作收束）、KVM、virtio/VFIO。

## 教学组件复用约定
- 所有 HTML（lesson / reference）共享 `../assets/style.css`。
- 测验统一用 `../assets/quiz.js`，quiz 答案选项**等长**（不漏线索）。
- 笔记统一用 `../assets/notes.js`（LR-0004）：右下角悬浮按钮开关抽屉；选中正文文字 → 弹"+ 记为笔记" → 自动引用选中内容；支持编辑/删除/导出 JSON/导入/复制为 Markdown。笔记存于浏览器 localStorage，按课隔离（key = `lkl-notes:<文件名>`）。
- 每课必须链接到 `reference/glossary.html` 和相邻课程，推荐一个精读源。
- **【课程对齐到三级标题】（LR-0008）**：课程结构与书目录对齐到 §X.Y.Z，覆盖判定以三级子节为最小单位。详见 `CURRICULUM_MAP.md`。每课页眉 chip 必须标章节号 + 所覆盖的三级子节。

## ⚠️ 调整课程顺序时的全量 checklist（LR-0009）
> 来源：LR-0008/0009 暴露的问题——曾在第 5、6 课之间插入"设计模式课"，但只改了 nav 链接，漏改了第 5 课正文里 4 处"下一课进入内存管理篇"的预告，造成链接对、正文错的不一致。以后**任何**调整课程顺序/插入新课的操作，必须按下表全量排查，不许只改链接：

- [ ] **当前课页眉 chip**：去掉不再成立的标签（如"终章""最后一站"）。
- [ ] **当前课开头**：检查"这是 XX 篇的最后一站""下一课进入 XX"之类的前置预告。
- [ ] **当前课结尾 callout**：标题"下一站:XX"和正文里"第 N 课起进入 XX"。
- [ ] **当前课问题列表**：`<h3>带着这些问题进入下一课(XX)</h3>` 的括号主题与问题内容。
- [ ] **当前课 footer**：那段"准备好就说继续——下一课…"的灰色提示。
- [ ] **nav 链接**：`<p class="lesson-nav">` 里指向下一课的 href 与文字。
- [ ] **下游相邻课**：它的"上一课 ←"反向链接、页眉 chip、开头措辞是否仍成立。
- [ ] **CURRICULUM_MAP.md**：把对应三级子节的状态从 🔜 翻成 ✅/🟡，更新缺口优先级。
- [ ] **glossary.html**：新增术语、术语里的"第 N 课"交叉引用是否需更新。
- [ ] **自动化校验**：改完跑一遍 nav 链一致性检查（脚本见 LR-0009 历史）。

## 待观察 / 待确认
- 用户更偏图解还是代码精读？目前假设两者并重，几课后据反馈调整。
- 第 7 课内核同步（附录 B 13 子节）：一课讲全 vs 拆两课（基础 + RCU 单独）。倾向拆两课。
