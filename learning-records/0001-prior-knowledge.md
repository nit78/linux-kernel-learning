# 用户先验知识与学习起点（LR-0001）

**Status:** active

用户自述背景：C 语言和单片机扎实；有嵌入式 Linux **蓝牙驱动**和 **WiFi 驱动移植**经验；明确缺少的是"内核驱动框架与机制的深入理解"。学习目标是**晋升内核 / BSP 岗位**，现有 Linux/QEMU 动手环境。

## Evidence
- 自述"少了解 linux 内核驱动，熟悉 c 语言和单片机"——"了解"是弱词，但"移植经验"是强信号：说明已接触过设备树、Kbuild、`module_init`、加载流程，知道驱动的"形"。
- 拒绝自选起点，要求评估——说明对内核知识版图没有完整心智地图，需要引导。

## Implications（决定接下来怎么教）
1. **起点 = 本书第 2 章（数据结构与设计模式）的 `list_head` / 侵入式链表**：它是后续所有章节（内存 LRU、进程链表、VFS、设备模型）的高频地基，且对 MCU 工程师是清晰的**概念跃迁**（侵入式 vs 带数据链表 + `container_of`）。
2. **每课用"MCU / 裸机 思维 ↔ 内核 思维"对照**：这是用户最大的迁移杠杆，记入 NOTES.md。
3. **可直接上 `.ko` 实验**：用户有移植经验，不用从"什么是内核模块"讲起，实验可一步到位。
4. **暂不收 upstream 流程资源**：目标是先补"理解 + 定位"能力，社区贡献列入 out of scope。
5. **三大核心中，内存管理篇价值最高**：驱动开发的头号痛点，待知识储备篇完成后优先进入。
